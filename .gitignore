# --- Angular / Node ---
node_modules/
dist/
dist-ssr/
.angular/
.angular/cache/
out-tsc/
tmp/
temp/

# TS incremental build info
*.tsbuildinfo

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# --- Testing artifacts ---
coverage/
.nyc_output/
.vitest/
.karma/
*.lcov
reports/
test-results/

# --- Linters / caches ---
.eslintcache
.stylelintcache
.sass-cache/

# --- IDE / Editor junk ---
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.iml
.vs/

# --- OS junk ---
.DS_Store
Thumbs.db

# --- Env / local-only config (keep real secrets out of git) ---
.env
.env.*.local

# Optional: ignore per-dev Angular env files (commit the default ones, ignore *local*)
src/environments/*.local.ts

# Optional: if you keep per-dev proxy overrides, ignore the local variant (keep proxy.conf.json tracked)
proxy.conf.local.json
